<Window x:Class="Linear_Programming_Calculator_Desktop.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Linear_Programming_Calculator_Desktop"
        mc:Ignorable="d"
        Title="Задачі лінійного програмування" Closed="Window_Closed" SizeToContent="Width" Height="450" Loaded="Window_Loaded" Width="800" Background="#F2F9F9F9" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/LinearProgStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" Grid.Column="0" Grid.Row="0" Margin="10" />
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column ="0" Grid.Row="1">
            <StackPanel x:Name="SolutionPanel" Margin="10" />
        </ScrollViewer>
    </Grid>
</Window>
