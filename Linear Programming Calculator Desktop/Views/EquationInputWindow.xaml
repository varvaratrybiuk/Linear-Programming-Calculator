<Window x:Class="Linear_Programming_Calculator_Desktop.EquationInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Linear_Programming_Calculator_Desktop"
        mc:Ignorable="d"
        Title="Задачі лінійного програмування" Closed="Window_Closed" SizeToContent="WidthAndHeight" Loaded="Window_Loaded" WindowStartupLocation="CenterOwner" ResizeMode="CanMinimize" Background="#F2F9F9F9">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/LinearProgStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel x:Name="TopPanel" Grid.Row="0" Margin="10"/>
            <Border Grid.Row="1" Background="#FFFFF0" CornerRadius="5" BorderThickness="1" BorderBrush="#FFD966" Padding="10">
                <StackPanel x:Name="ConstraintPanel"/>
            </Border>
            
        </Grid>
    </ScrollViewer>
</Window>
